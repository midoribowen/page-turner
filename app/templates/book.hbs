{{#paper-card}}
  <img src="{{model.image_url}}" alt="{{model.title}}" />
  {{#paper-card-content}}
    <h1>{{model.title}}</h1>
    <h2>{{model.author}}</h2>
    <p>
      {{model.summary}}
    </p>
  {{/paper-card-content}}
{{/paper-card}}
