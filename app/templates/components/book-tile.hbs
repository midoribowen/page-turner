{{#paper-card}}
  <img src="{{book.image_url}}" alt="{{book.title}}" />
  {{#paper-card-content}}
    <h2>{{book.title}}</h2>
    <h3>{{book.author}}</h3>
    <p>
      {{book.summary}}
    </p>
  {{/paper-card-content}}
{{/paper-card}}
